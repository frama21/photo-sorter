# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Firebase Hosting config (firebase.json + .firebaserc) is version-controlled
# like the Dockerfile — it carries the production security headers. Only the
# local build cache and debug logs are ignored.
.firebase
firebase-debug.log

.claude